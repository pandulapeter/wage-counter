# presentation:hourlyWage module
This Android feature module defines the presentation layer of the "hourlyWage" feature.

**Depends on**
- `:domain` (transitively also on `:data:model`)
- `:presentation:shared`
- `:presentation:debugMenu`

**Exposes API for**
- `:presentation:main`