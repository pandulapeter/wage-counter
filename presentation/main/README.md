# presentation:main module
This Android feature module defines the presentation layer of the "main" feature.

**Depends on**
- `:domain` (transitively also on `:data:model`)
- `:presentation:debugMenu`

**Exposes API for**
- `:app`